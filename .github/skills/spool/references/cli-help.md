# CLI help

Inspect the installed command surface and flags instead of assuming a release supports a feature:

```sh
spl --help
spl <command> --help
spl merge --help
```

Generate a completion script with:

```sh
spl completion <bash|zsh|fish|powershell>
```
