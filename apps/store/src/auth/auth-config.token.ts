/**
 * DI token for the `AuthConfig` value provided by AuthModule. See `pg-pool.token.ts` for the
 * same symbol-token convention used elsewhere in this codebase.
 */
export const AUTH_CONFIG = Symbol('AUTH_CONFIG');
