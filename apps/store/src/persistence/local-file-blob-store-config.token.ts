/**
 * DI token for the `LocalFileBlobStoreConfig` value provided by PersistenceModule. See
 * `pg-pool.token.ts` for the same symbol-token convention used elsewhere in this codebase.
 */
export const LOCAL_FILE_BLOB_STORE_CONFIG = Symbol('LOCAL_FILE_BLOB_STORE_CONFIG');
