/**
 * DI token for the `ArtifactDownloadConfig` value provided by `ArtifactsModule`. See
 * `pg-pool.token.ts` for the same symbol-token convention used elsewhere in this codebase.
 */
export const ARTIFACT_DOWNLOAD_CONFIG = Symbol('ARTIFACT_DOWNLOAD_CONFIG');
